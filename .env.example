# .env.example
# This file contains the required environment variables for the application.
# Copy this file to .env.local and fill in the values.
# IMPORTANT: Do not commit .env.local to your version control.

# Firebase Client Configuration (for the browser)
# These are safe to be public.
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Firebase Admin Configuration (for the server)
# These are secrets and must NOT be exposed to the public.
# Go to your Firebase Project Settings -> Service Accounts -> Generate new private key
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
# Copy the entire private key, including the -----BEGIN PRIVATE KEY----- and -----END PRIVATE KEY-----
# Then, format it for a .env file by replacing all newlines with \n
FIREBASE_PRIVATE_KEY=

# Other Service Keys (optional, for future migrations)
RESEND_API_KEY=
REDIS_URL=
PINECONE_API_KEY=
