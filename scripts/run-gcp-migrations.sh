#!/bin/bash
#
# Firestore Migration Placeholder
#
# This project now uses Firestore, which does not require traditional
# database migrations. This script is retained for compatibility with
# existing deployment pipelines and exits without performing any action.

set -euo pipefail

echo "No database migrations are required for Firestore."
